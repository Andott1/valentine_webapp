name: valentine_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  audioplayers: ^6.5.1
  file_saver: ^0.3.1
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.3
  google_fonts: ^8.0.1
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/sounds/
    - assets/flowers/